name: Build iOS App (No Mac Required)

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build-ios:
    runs-on: macos-14
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install Global Dependencies
        run: |
          npm install -g cordova@latest

      - name: Install Project Dependencies
        run: |
          npm install --package-lock=false

      - name: Add & Prepare iOS Platform
        run: |
          npx cordova platform add ios --save || true
          npx cordova prepare ios

      - name: Build iOS App
        run: |
          # Build unsigned for physical iOS devices (arm64 iphoneos)
          npx cordova build ios --device -- --codeSignIdentity="" --codesigningAllowed=No || npx cordova build ios --emulator
          
          # Create Payload directory for sideloading format
          mkdir -p Payload
          # Prioritize real device build (iphoneos)
          APP_PATH=$(find platforms/ios/build -name "*.app" -type d | grep -i "iphoneos" | head -n 1)
          if [ -z "$APP_PATH" ]; then
            APP_PATH=$(find platforms/ios/build -name "*.app" -type d | head -n 1)
          fi
          
          if [ -n "$APP_PATH" ]; then
            echo "Packaging app found at: $APP_PATH"
            cp -r "$APP_PATH" Payload/
            zip -r EternalLife-iOS.ipa Payload/
          else
            echo "Warning: No .app directory found"
          fi
          
          # Compress Xcode project folder for Mac users
          zip -r EternalLife-Xcode-Project.zip platforms/ios

      - name: Upload iOS App Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: EternalLife-iOS-Build
          path: |
            EternalLife-iOS.ipa
            EternalLife-Xcode-Project.zip

